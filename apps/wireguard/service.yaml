apiVersion: v1
kind: Service
metadata:
  name: wireguard
spec:
  type: NodePort
  selector:
    app: wireguard
  ports:
    - name: wireguard
      port: 51820
      protocol: UDP
      targetPort: 51820
      nodePort: 31820
