apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: syncthing-mnt
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: syncthing-mnt
  resources:
    requests:
      storage: 16Ti
