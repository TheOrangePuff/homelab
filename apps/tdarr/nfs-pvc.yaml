apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr-media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: tdarr-media
  resources:
    requests:
      storage: 16Ti
