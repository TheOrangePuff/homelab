apiVersion: v1
kind: Service
metadata:
  name: tdarr
spec:
  selector:
    app: tdarr
  type: ClusterIP
  ports:
  - name: web-ui
    port: 8265
    targetPort: 8265
    protocol: TCP
  - name: server
    port: 8266
    targetPort: 8266
    protocol: TCP
