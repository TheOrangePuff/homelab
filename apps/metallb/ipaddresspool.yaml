apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: default-pool
  namespace: metallb-system
spec:
  addresses:
  # TODO: create a new subnet range for this
  - 192.168.0.240-192.168.0.250
