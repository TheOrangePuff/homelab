apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tdarr
  namespace: flux-system
spec:
  interval: 1m
  path: ./apps/tdarr
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: app
  dependsOn:
    - name: app-namespace
    - name: metallb
    - name: traefik
    - name: longhorn
