apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: syncthing
  namespace: flux-system
spec:
  interval: 1m
  path: ./apps/syncthing
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: default
